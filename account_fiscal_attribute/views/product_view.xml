<openerp>
    <data>

        <record id="view_product_fiscal_property_form" model="ir.ui.view">
            <field name="name">product.template.inherit.fiscal.property.form</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_form_view"/>
            <field name="arch" type="xml">
                <page string="Accounting" position="after">
                    <page string="Fiscal Properties" col="4" name="fiscal_properties" groups="account.group_account_user">
                        <group string="Fiscal Properties" col="4" colspan="4">
                            <field name="property_fiscal_attribute" />   <!-- "field does not exist?!?!?" -->
                        </group>
                    </page>
                </page>
            </field>
        </record>

    </data>
</openerp>